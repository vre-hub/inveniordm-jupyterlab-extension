/**
 * Module that exports hooks, getters and setters for storing values
 * in this extension.
 * E.g. to store user settings, cache values and avoid passing props down the component tree.
 * Internally uses Zustand for state management.
 */
export * from './runtimeStore';
export * from './uiStore';
