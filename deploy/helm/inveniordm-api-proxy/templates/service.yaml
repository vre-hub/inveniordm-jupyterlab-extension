apiVersion: v1
kind: Service
metadata:
  name: {{ include "inveniordm-api-proxy.fullname" . }}
  labels:
    {{- include "inveniordm-api-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inveniordm-api-proxy.selectorLabels" . | nindent 4 }}
